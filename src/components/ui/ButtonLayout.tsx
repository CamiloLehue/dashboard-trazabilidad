export function ButtonLayout({ children }: { children: React.ReactNode }) {
    return (
        <div className="w-full p-10 rounded-lg bg-white shadow">
            {children}
        </div>
    )
}
